{
  "fixture_id": "express.cookie-conflicting-writes",
  "framework": "express",
  "source_files": ["app.ts"],
  "expected_artifacts": ["session_cookie:session"],
  "expected_lifecycle_stages": ["store", "transmit"],
  "expected_findings": ["cookie_conflicting_writes_review"],
  "notes": "P1 cookie hardening fixture for cookie-conflicting-writes. Uses placeholder values only; Django runtime coverage is narrower than settings-derived cookie coverage where applicable."
}
